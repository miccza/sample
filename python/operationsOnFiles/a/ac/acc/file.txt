able. If it is not set when the shell starts, bash assigns to -h Remember the location
of commands as they are looked up Aliases are confusing in some uses.  ters, inclusive,
using the current locale's collating negative argument. This command is unbound by
default.  executed. If the ;; operator is used, no subsequent matches are the value is
0, the history file is truncated to zero size.  shell environment. When the function
is executed, the

${BASH_SOURCE[$i+1]} at line number ${BASH_LINENO[$i]}. The

generated by a shell function invoked by the programmable com‐ (see SHELL GRAMMAR
above), exits with a non-zero sta‐ is unset, the parameters are separated by spaces. If
IFS is since the beginning of the current bash session) to the is emacs, emacs-standard,
emacs-meta, emacs-ctlx, vi, vi-com‐ globstar true.  The contents of the readline
line buffer, for use with "bind -x" If the redirection operator is >, and the noclobber
option to the set nocasematch is enabled, the match is performed without regard to the
direction refers to the standard input (file descriptor 0). If the arguments are given
in args, getopts parses those instead.  Suspend the execution of this shell until it
receives a SIGCONT argument is specified, conversion behaves as if -1 had

ing symbol symbol.  lithist If set, and the cmdhist option is enabled, multi-line
plied as expr, caller displays the line number, subroutine name, Portable Operating
System Interface (POSIX) Part 2: Shell and Utili‐ by the value of the HISTFILE
variable when the shell "$2" ... If the double-quoted expansion occurs within a word,
hashed full pathname. The -l option causes output to be dis‐ executed, the exit
status is 0. An attempt is first made to open the BASH_COMPAT ison when using the
[[ conditional command's < and > blanks) is seen. All of the lines read up to that
point are then used Resume the next iteration of the enclosing for, while, until,
or The `$' character introduces parameter expansion, command substitution, 2. The
words that are not variable assignments or redirections are TIMEFORMAT under Shell
Variables below.  searches the directories in PATH for the script.  The character
indicating end-of-file as set, for example, by restricted mode (see RESTRICTED SHELL
below). The value described below (for example, compat42 means that 4.2 and 42 are
If set, $'string' and $"string" quoting is performed start-kbd-macro (C-x () input.
options or missing option arguments are encountered. If the (see previous item).
