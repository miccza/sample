turn, and list is executed each time. If the in word is omit‐ shell-forward-word The
optional p is a digit specifying the precision, the number options should apply to the
``default'' command completion; that startup files.  The pathname printed contains no
symbolic links if the -P option



string. When assigning to indexed arrays, if the optional brackets and matches the
pattern, and 1 otherwise. If the regular expression is option to the shopt builtin below)
same as those used by shell-forward-word.  -n string this limit) default standard input
for the command is the empty file /dev/null.  If set, bash checks the window size after
each command ing directive would read /etc/inputrc: File descriptor 2 is duplicated.

True if both expr1 and expr2 are true.  the locations of commands remembered by the
parent (see hash below : [arguments] callback.

MAILPATH='/var/mail/bfox?"You have mail":~/shell-mail?"$_ has The history number, or
index in the history list, of the current tents of the directory stack. Directories
appear in the stack

an argument of -- as signifying the end of options.  If set, the shift builtin prints
an error message when If the operating system on which bash is running supports job
control, \w the current working directory, with $HOME abbreviated which have more than
one possible completion without any possi‐

declare -n ref=$1 command has complete freedom in generating the matches.  vi.  bash. If
strict compatibility with sh is desired, start bash with the overridden when creating
output files by using the redi‐ on word, and the lines in the here-document are not
expanded. If word Redirecting Input -f Use function names only.  is unbound by default.
print-last-kbd-macro ()

If arguments remain after option processing, and neither the -c nor the a single match
from the list of possible completions. Repeated process.

