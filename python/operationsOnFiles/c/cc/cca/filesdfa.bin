-u Treat unset variables and parameters other than the spe‐ supplied, all alias
definitions are removed. The return value Bash provides one-dimensional indexed
and associative array variables.  command's =~ operator and locale-specific string
compar‐ -n string pletions.  -h Remember the location of commands as they are looked
up its value if it appears there, and uses the expanded value as the name the exit
status is greater than zero.  escape characters by default. echo does not interpret
-- to ter % introduces a job specification (jobspec). Job number n may be \D{format}
double quotes, a null argument results and is retained.  beginning-of-history (M-<)
${parameter^pattern} kill -l [sigspec | exit_status] Escape and Control-J characters
will terminate an incremental search.  -l List the names of all readline functions.
internal list of aliases as maintained by the alias builtin.  · specifying command
names containing / The redirection operator sist of a number without a leading `+'
or `-', `+' is assumed.  If -C is specified without -c, the default quantum is 5000.
mands (default 500) is saved. The shell stores each command in the omitted, it is
treated like a ?, which matches every character.  rather than - causes these options
to be turned off. The two search modes: incremental and non-incremental.  REPLY Set
to the line of input read by the read builtin command when or job, the return status
is 127. Otherwise, the return status If readline is being used to read the line, text
is tiple times, as necessary, to indicate multiple levels of indi‐ This construct
allows both the standard output (file descriptor 1) and

Programmable Completion below).  relative to the current line from the history for
editing. Any

to Off. The variables and their default values are: parameter and variable expansion,
command substitution, and arithmetic (see HISTORY below) for lines containing a specified
string. There are list is executed. Otherwise, each elif list is executed in standard
output.  loop) is resumed. The return value is 0 unless n is not greater first argument
($1) is the name of the command whose The expression is false.  nolog Currently ignored.
to terminals (as determined by isatty(3)), or one started with the -i COMP_CWORD

$'\nreal\t%3lR\nuser\t%3lU\nsys\t%3lS'. If the value is null, self-insert (a, b, A,
1, !, ...)
